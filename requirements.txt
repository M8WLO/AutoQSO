# AutoQSO requirements
# Install with: pip install -r requirements.txt

# GUI
PyQt6>=6.6.0

# Audio I/O
pyaudio>=0.2.14
sounddevice>=0.4.6
soundfile>=0.12.1

# STT — install ONE of these:
faster-whisper>=1.0.0     # preferred: faster, lower memory
# openai-whisper>=20240930  # fallback: pip install openai-whisper

# TTS
pyttsx3>=2.90              # offline Windows SAPI (primary)
edge-tts>=6.1.9            # optional neural TTS (requires internet)

# Serial port enumeration
pyserial>=3.5

# Logging
requests>=2.31.0
